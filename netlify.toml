[[headers]]
  for = "/*"
  [headers.values]
    X-Robots-Tag = "noindex, nofollow"

# Custom bot block via redirects is disabled for now (using Netlify extension only).
# To enable this rule again, uncomment the block below.
#
# [[redirects]]
#   from = "/_next/image*"
#   to = "/api/blocked"
#   status = 403
#   conditions = {User_Agent = ["ClaudeBot", "anthropic-ai", "claudebot"]}